from django.urls import path
from . import views

urlpatterns = [
    # path('routes/', views.route_list, name='route_list'),
    # path('routes/<int:pk>/', views.route_detail, name='route_detail'),
    # path('users/', views.user_data_list, name='user_data_list'),
    # path('users/<int:pk>/', views.user_data_detail, name='user_data_detail'),
]
